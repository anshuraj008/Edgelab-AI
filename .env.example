# Google Gemini API Key (Server-side only, never prefix with NEXT_PUBLIC)
GEMINI_API_KEY=
GEMINI_MODEL=

# Neon PostgreSQL Database URL (Optional - falls back gracefully to in-memory/browser state)
DATABASE_URL=

# Application Environment
NODE_ENV=development
